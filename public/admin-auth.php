<?php
declare(strict_types=1);

function loadDotEnvFile(string $path): void
{
    if (!is_file($path)) {
        return;
    }

    $lines = file($path, FILE_IGNORE_NEW_LINES | FILE_SKIP_EMPTY_LINES);
    if (!is_array($lines)) {
        return;
    }

    foreach ($lines as $line) {
        $trimmed = trim($line);
        if ($trimmed === '' || str_starts_with($trimmed, '#')) {
            continue;
        }
        $parts = explode('=', $trimmed, 2);
        if (count($parts) !== 2) {
            continue;
        }
        $key = trim($parts[0]);
        $value = trim($parts[1]);
        if ($key === '') {
            continue;
        }
        if ((str_starts_with($value, '"') && str_ends_with($value, '"'))
            || (str_starts_with($value, "'") && str_ends_with($value, "'"))
        ) {
            $value = substr($value, 1, -1);
        }
        if (getenv($key) === false) {
            putenv($key . '=' . $value);
            $_ENV[$key] = $value;
            $_SERVER[$key] = $value;
        }
    }
}

loadDotEnvFile(dirname(__DIR__) . DIRECTORY_SEPARATOR . '.env');
loadDotEnvFile(__DIR__ . DIRECTORY_SEPARATOR . '.env');

// Session key can stay static, but password should come from .env.
const ADMIN_SESSION_KEY = 'valdorio_admin_ok';
define('ADMIN_PASSWORD', trim((string)(getenv('ADMIN_PASSWORD') ?: '')));
